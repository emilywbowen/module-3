import React from "react";
import DiceBox from "./DiceBox";


export default function Die(){
   <div>
        // here is where a random number received as a prop is displayed. The user can click on a die (need a box with an radio selector) to roll the non-selected die again. 
   </div>
}